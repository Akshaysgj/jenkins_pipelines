pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo "code is changing"
            }

        }
        stage('build'){
            steps {
                echo "build with the help of maven"
            }
        }
        stage('deploy'){
            steps{
                echo "deploy to the applications"
            }
        }
    }
}
